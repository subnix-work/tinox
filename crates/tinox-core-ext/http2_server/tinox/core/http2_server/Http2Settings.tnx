module tinox.core.http2_server;

namespace tinox.core.http2_server
{
    // =========================================================================
    // SETTINGS parameter identifiers (RFC 7540 §6.5.2)
    // =========================================================================

    class Http2Settings
    {
        fn HEADER_TABLE_SIZE()      -> Int64 { return 0x1; }
        fn ENABLE_PUSH()            -> Int64 { return 0x2; }
        fn MAX_CONCURRENT_STREAMS() -> Int64 { return 0x3; }
        fn INITIAL_WINDOW_SIZE()    -> Int64 { return 0x4; }
        fn MAX_FRAME_SIZE()         -> Int64 { return 0x5; }
        fn MAX_HEADER_LIST_SIZE()   -> Int64 { return 0x6; }
    }
}
