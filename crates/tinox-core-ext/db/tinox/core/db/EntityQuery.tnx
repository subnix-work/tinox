module tinox.core.db;

namespace tinox.core.db
{
    // Stub class — all methods are intercepted by the codegen for @Entity classes.
    // The return types use Any to bypass strict type checking; actual types are
    // determined at compile time by the ORM chain interceptor.
    class EntityQuery
    {
        fn filter(predicate: fnc(Any) -> Bool) -> Any
        {
            return this;
        }

        fn orderBy(field: fnc(Any) -> Any) -> Any
        {
            return this;
        }

        fn orderByDesc(field: fnc(Any) -> Any) -> Any
        {
            return this;
        }

        fn limit(n: Int64) -> Any
        {
            return this;
        }

        fn offset(n: Int64) -> Any
        {
            return this;
        }

        fn list() -> Any
        {
            return 0;
        }

        fn first() -> Any
        {
            return 0;
        }

        fn count() -> Int64
        {
            return 0;
        }

        fn save(entity: Any) -> Any
        {
            return entity;
        }

        fn delete(entity: Any) -> Nothing
        {
        }

        fn saveAll(entities: Any) -> Nothing
        {
        }
    }
}
