// expect: 3628800

class Main
{
    fnc factorial(n: Int64) -> Int64
    {
        if (n <= 1) { return 1; }
        return n * factorial(n - 1);
    }

    fnc main() -> Int32
    {
        println(factorial(10));
        return 0;
    }
}
