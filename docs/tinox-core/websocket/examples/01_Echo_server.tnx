import tinox.core.websocket;

class Main
{
    fnc main() -> Int32
    {
        let srv: Int64 = WsServer::listen(8080);
        let conn: Int64 = WsServer::accept(srv);

        while true
        {
            let f: WsFrame = Ws::readMessage(conn);
            if (f.opcode != 1)
            {
                break;
            }
            Ws::sendText(conn, Ws::text(f));
        }

        Ws::close(conn);
        return 0;
    }
}
