[package]
name = "amqp091-fuzz-driver"
version = "0.1.0"
description = "Local-only manifest so Amqp091Driver.tnx can pull in the real tinox.core.amqp091 module as a dependency."

[[dependencies]]
group = "tinox.core"
artifactId = "amqp091"
version = "1.0.3"
