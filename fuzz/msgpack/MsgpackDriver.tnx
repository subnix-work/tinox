import tinox.core.msgpack;

class MsgpackDriver
{
    fnc tinoxMsgpackDecode(bytes: List<Int64>) -> Int64
    {
        let value: MsgpackValue = Msgpack::decode(bytes);
        if (value.isNull())
        {
            return 0;
        }
        return 1;
    }
}
